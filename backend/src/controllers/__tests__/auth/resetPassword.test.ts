//Throws error when token does not exist in DB or resetPasswordToken is null
//Throws error when password is null (facebook, google account)
//Throws error when token has expired
//Updates password, and nulifies token and expiration
test.skip("skip", () => {});
