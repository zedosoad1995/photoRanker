//Throws error when user not logged
//Throws error when user is Admin
//Deletes logged regular user and clears session cookies
// Check if all pics were deleted (Storage)
test.skip("skip", () => {});
