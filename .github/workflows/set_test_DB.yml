name: Set Test DB

on: [pull_request]

jobs:
  set_test_DB:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build the stack
        run: docker-compose --file="infra/dockers/docker-compose-postgres-test.yaml" up -d
